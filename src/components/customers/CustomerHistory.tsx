'use client'
import type { Customer, Sale, RepairOrder } from '@/types'
interface Props { customer: Customer; sales: Sale[]; repairOrders: RepairOrder[]; onClose: () => void; isUsta: boolean }
export function CustomerHistory({ customer, sales, repairOrders, onClose, isUsta }: Props) {
  return <div className="fixed inset-0 bg-black/40 backdrop-blur-sm z-50 flex items-center justify-center p-4" onClick={onClose}><div className="bg-white dark:bg-gray-800 rounded-2xl p-6 w-full max-w-lg max-h-[80vh] overflow-y-auto" onClick={e=>e.stopPropagation()}><h3 className="text-lg font-bold mb-4">📋 {customer.ism} — Tarix</h3>{isUsta?<div className="space-y-3">{repairOrders.length===0?<p className="text-gray-400 text-center py-8">Tamirlash tarixi yo'q</p>:repairOrders.map(r=><div key={r.id} className="p-3 bg-gray-50 dark:bg-gray-700/50 rounded-lg"><div className="font-semibold text-sm">{r.qurilma}</div><div className="text-xs text-gray-500">{r.sana} · {r.muammo}</div><span className="text-xs font-bold px-2 py-0.5 rounded-full bg-blue-100 text-blue-700">{r.holat}</span></div>)}</div>:<div className="space-y-3">{sales.length===0?<p className="text-gray-400 text-center py-8">Sotuv tarixi yo'q</p>:sales.map(s=><div key={s.id} className="p-3 bg-gray-50 dark:bg-gray-700/50 rounded-lg flex justify-between"><div><div className="font-semibold text-sm">{s.chek}</div><div className="text-xs text-gray-500">{s.sana}</div></div><div className="font-bold text-green-600 text-sm">{Math.round(s.jami_usd*12700).toLocaleString()} so'm</div></div>)}</div>}<button onClick={onClose} className="w-full mt-4 py-2 border rounded-lg text-sm font-medium">Yopish</button></div></div>
}