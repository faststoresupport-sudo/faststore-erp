'use client'
import type { Customer, RepairOrder } from '@/types'
interface Props { customers: Customer[]; repairOrders: RepairOrder[]; onAddRepair: (id: number) => void; onViewHistory: (c: Customer) => void; onEditCustomer: (c: Customer) => void }
export function UstaMijozlar({ customers, repairOrders, onAddRepair, onViewHistory, onEditCustomer }: Props) {
  if (customers.length === 0) return <div className="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl p-8 text-center"><div className="text-4xl mb-3">👥</div><div className="font-medium text-gray-500">Mijoz topilmadi</div></div>
  return <div className="grid grid-cols-1 md:grid-cols-2 gap-4">{customers.map(c=>{const repairs=repairOrders.filter(r=>r.customer_id===c.id);const active=repairs.filter(r=>['qabul_qilindi','diagnostika','tamirlashda'].includes(r.holat));return <div key={c.id} className="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl p-5 shadow-sm"><div className="flex items-center gap-3 mb-4"><div className="w-11 h-11 rounded-full bg-gradient-to-br from-orange-400 to-orange-600 flex items-center justify-center text-white font-bold text-lg">{c.ism[0]}</div><div><div className="font-bold text-gray-900 dark:text-white">{c.ism}</div><div className="text-xs text-gray-500">📞 {c.telefon}</div></div></div><div className="grid grid-cols-2 gap-3 mb-4"><div className="bg-gray-50 dark:bg-gray-700/50 rounded-lg p-3 text-center"><div className="text-xs text-gray-400">JAMI</div><div className="font-bold text-blue-600">{repairs.length}</div></div><div className="bg-gray-50 dark:bg-gray-700/50 rounded-lg p-3 text-center"><div className="text-xs text-gray-400">FAOL</div><div className="font-bold text-orange-500">{active.length}</div></div></div><div className="flex gap-2"><button onClick={()=>onAddRepair(c.id)} className="flex-1 py-2 bg-orange-500 text-white rounded-lg text-xs font-bold hover:bg-orange-600 transition-colors">🔧 Buyurtma</button><button onClick={()=>onViewHistory(c)} className="flex-1 py-2 border border-gray-200 dark:border-gray-600 rounded-lg text-xs font-medium hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">📋 Tarix</button></div></div>})}</div>
}