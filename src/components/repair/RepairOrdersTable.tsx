'use client'
import type { RepairOrder } from '@/types'
interface Props { orders: RepairOrder[]; loading: boolean; onEdit: (o: RepairOrder) => void; onChangeStatus: (id: number, s: string) => void; onPrintReceipt: (o: RepairOrder) => void; onViewReceipt: (o: RepairOrder) => void; statuses: any[] }
export function RepairOrdersTable({ orders, loading, onEdit, onChangeStatus, onPrintReceipt, statuses }: Props) {
  if (loading) return <div className="p-8 text-center text-gray-400">⏳</div>
  if (orders.length === 0) return <div className="bg-white dark:bg-gray-800 border rounded-xl p-8 text-center"><div className="text-4xl mb-3">🔧</div><div className="text-gray-500">Buyurtma yo'q</div></div>
  return <div className="space-y-3">{orders.map(o=>{const h=statuses.find((s: any)=>s.value===o.holat);return <div key={o.id} className="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden shadow-sm"><div className="p-4 flex items-center gap-4"><div className="w-12 h-12 rounded-xl bg-gradient-to-br from-orange-100 to-orange-200 dark:from-orange-900/30 dark:to-orange-800/30 flex items-center justify-center text-xl flex-shrink-0">📱</div><div className="flex-1 min-w-0"><div className="flex items-center gap-2 mb-1"><span className="font-bold text-sm">{o.qurilma}</span><span className={`text-xs px-2 py-0.5 rounded-full font-bold ${o.holat==='tayyor'?'bg-green-100 text-green-700':o.holat==='tamirlashda'?'bg-blue-100 text-blue-700':'bg-orange-100 text-orange-700'}`}>{h?.label||o.holat}</span></div><div className="text-xs text-gray-500">👤 {o.mijoz} · 📞 {o.telefon} · 📅 {o.sana}</div><div className="text-xs text-gray-600 dark:text-gray-400 mt-1">🔍 {o.muammo}</div></div><div className="text-right flex-shrink-0"><div className="font-bold text-green-600">{o.narx_usd>0?Math.round(o.narx_usd*12700).toLocaleString()+' so\'m':'—'}</div></div></div><div className="px-4 py-2 bg-gray-50 dark:bg-gray-700/30 border-t border-gray-100 dark:border-gray-700 flex gap-2 flex-wrap"><button onClick={()=>onEdit(o)} className="text-xs px-3 py-1.5 bg-white dark:bg-gray-700 border rounded-lg font-medium">✏️ Tahrir</button><button onClick={()=>onPrintReceipt(o)} className="text-xs px-3 py-1.5 bg-white dark:bg-gray-700 border rounded-lg font-medium">🖨️ Chek</button>{o.holat==='qabul_qilindi'&&<button onClick={()=>onChangeStatus(o.id,'diagnostika')} className="text-xs px-3 py-1.5 bg-orange-500 text-white rounded-lg font-bold">🔍 Diagnostika</button>}{o.holat==='diagnostika'&&<button onClick={()=>onChangeStatus(o.id,'tamirlashda')} className="text-xs px-3 py-1.5 bg-blue-500 text-white rounded-lg font-bold">🔧 Tamirlash</button>}{o.holat==='tamirlashda'&&<button onClick={()=>onChangeStatus(o.id,'tayyor')} className="text-xs px-3 py-1.5 bg-green-500 text-white rounded-lg font-bold">✅ Tayyor</button>}{o.holat==='tayyor'&&<button onClick={()=>onChangeStatus(o.id,'topshirildi')} className="text-xs px-3 py-1.5 bg-purple-500 text-white rounded-lg font-bold">🏁 Topshirildi</button>}</div></div>})}</div>
}