'use client'
import type { RepairOrder } from '@/types'
interface Props { order: RepairOrder; onClose: () => void; onPrint: () => void; statuses: any[] }
export function RepairReceiptModal({ order, onClose, onPrint, statuses }: Props) {
  const h = statuses.find(s => s.value === order.holat)
  return <div className="fixed inset-0 bg-black/40 backdrop-blur-sm z-50 flex items-center justify-center p-4" onClick={onClose}><div className="bg-white dark:bg-gray-800 rounded-2xl p-6 w-full max-w-sm" onClick={e=>e.stopPropagation()}><h3 className="text-lg font-bold mb-4 text-center">🔧 Buyurtma #{order.id}</h3><div className="bg-gray-50 dark:bg-gray-700 rounded-xl p-4 font-mono text-sm space-y-2"><div className="text-center font-bold text-lg">🔧 FastStore Servis</div><hr className="border-dashed"/><div className="flex justify-between"><span>Mijoz:</span><span className="font-bold">{order.mijoz}</span></div><div className="flex justify-between"><span>Qurilma:</span><span className="font-bold">{order.qurilma}</span></div><div className="flex justify-between"><span>Holat:</span><span className="font-bold">{h?.label||order.holat}</span></div><hr className="border-dashed"/><div className="flex justify-between font-bold"><span>Narx:</span><span className="text-green-600">{order.narx_usd>0?Math.round(order.narx_usd*12700).toLocaleString()+' so\'m':'Aniqlanmagan'}</span></div></div><div className="flex gap-3 mt-4"><button onClick={onPrint} className="flex-1 py-2 bg-orange-500 text-white rounded-lg text-sm font-bold">🖨️ Print</button><button onClick={onClose} className="flex-1 py-2 border rounded-lg text-sm font-medium">Yopish</button></div></div></div>
}